#include <stdio.h>

int main(){

    // for loop = repeats a section of a code for a speecific amount of times

    for(int index = 1; index <=10; index++){
        printf("%d\n", index);
    }

    /*
    for(declaration and initialization of controller/index; condtion till which to repeat; when when to perfor loop){
        body of loop containing code which is to be iterated
    }
    */

    return 0;
}